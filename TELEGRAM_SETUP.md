# 📱 Настройка Telegram уведомлений

Система поддерживает отправку уведомлений в Telegram о звонках, обновлениях статусов задач и ошибках.

## 🚀 Быстрый старт

### 1. Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например, "ClickUp Reminders Bot")
   - Введите username бота (должен заканчиваться на `bot`, например, `clickup_reminders_bot`)
4. BotFather пришлет вам токен бота в формате: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`
5. **Сохраните этот токен** - он понадобится для конфигурации

### 2. Получение Chat ID

#### Для личных сообщений:
1. Найдите вашего бота в Telegram по username
2. Нажмите "Start" или отправьте любое сообщение
3. Откройте в браузере: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
   - Замените `<YOUR_BOT_TOKEN>` на токен вашего бота
4. Найдите в ответе поле `"chat":{"id":123456789}` - это ваш Chat ID

#### Для группы:
1. Создайте группу в Telegram
2. Добавьте вашего бота в группу (через меню → Add members)
3. Отправьте любое сообщение в группу
4. Откройте в браузере: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
5. Найдите в ответе `"chat":{"id":-123456789}` - это Chat ID группы (обычно отрицательное число)

**Альтернативный способ** - используйте бота [@userinfobot](https://t.me/userinfobot):
- Для личного ID: просто напишите боту
- Для группы: добавьте бота в группу, он покажет ID группы

### 3. Конфигурация

#### 3.1. Обновите `config.json`

Добавьте секцию `telegram`:

```json
{
  "clickup_workspace_id": "your_workspace_id",
  "reminder_list_name": "Напоминания",
  "reminder_tag": "#напоминание",
  "phone_mapping": {
    "Alex": "+1234567890"
  },
  "working_hours": {
    "start": 9,
    "end": 18
  },
  "telegram": {
    "enabled": true,
    "chat_id": "123456789",
    "assignee_chat_ids": {
      "Alex": "111111111",
      "Eve": "222222222"
    },
    "notifications": {
      "task_reminders": true,
      "status_updates": true,
      "call_notifications": true,
      "sms_notifications": true,
      "errors": true
    }
  }
}
```

**Параметры:**
- `enabled` - включить/выключить Telegram уведомления
- `chat_id` - ID чата или группы для отправки уведомлений
- `assignee_chat_ids` - словарь "Имя исполнителя" → "Chat ID" для персональных напоминаний (опционально)
- `notifications` - настройка типов уведомлений:
  - `task_reminders` - напоминания о задачах
  - `status_updates` - обновления статусов задач
  - `call_notifications` - уведомления о звонках
  - `sms_notifications` - уведомления об отправке SMS
  - `errors` - уведомления об ошибках

В корне секции ClickUp добавлен параметр `reminder_tag`. Он указывает, какой тег в ClickUp нужно использовать для напоминаний. По умолчанию система ищет тег `#напоминание` во всех пространствах и отправляет задачи с этим тегом в Telegram. Если укажете собственный тег (строку или список строк), бот соберёт задачи по нему и только при отсутствии таких задач вернётся к списку `reminder_list_name`.

#### 3.2. Обновите `secrets.json`

Добавьте токен бота в файл секретов (обычно `.venv/bin/secrets.json`):

```json
{
  "clickup": {
    "api_key": "your_clickup_api_key",
    "team_id": "your_team_id"
  },
  "twilio": {
    "account_sid": "your_twilio_sid",
    "auth_token": "your_twilio_token",
    "phone_number": "+1234567890"
  },
  "openai": {
    "api_key": "your_openai_key"
  },
  "telegram": {
    "bot_token": "1234567890:ABCdefGHIjklMNOpqrsTUVwxyz",
    "chat_id": "123456789"
  }
}
```

**Примечание:** `chat_id` можно указать либо в `config.json`, либо в `secrets.json`. Если указан в обоих местах, приоритет имеет значение из `config.json`.

### 4. Проверка настройки

Создайте тестовый скрипт `test_telegram.py`:

```python
#!/usr/bin/env python3
import json
from telegram_notifier import create_telegram_notifier

# Загрузка конфигурации
with open('config.json', 'r') as f:
    config = json.load(f)

with open('.venv/bin/secrets.json', 'r') as f:
    secrets = json.load(f)

# Создание notifier
telegram = create_telegram_notifier(config, secrets)

if telegram:
    print("✅ Telegram notifier успешно создан")
    
    # Тестовое сообщение
    result = telegram.send_message("🎉 Тестовое сообщение от системы напоминаний!")
    
    if result.get("ok"):
        print("✅ Сообщение успешно отправлено!")
    else:
        print(f"❌ Ошибка: {result.get('description')}")
else:
    print("❌ Не удалось создать Telegram notifier")
```

Запустите:
```bash
python3 test_telegram.py
```

## 📋 Типы уведомлений

### 1. Уведомления о звонках
Отправляются при инициации звонка через Twilio:
```
📞 Уведомление о звонке

📋 Задача: [Alex] Проверить отчет
👤 Исполнитель: Alex
📱 Телефон: +1234567890
📊 Статус: initiated
```

### 2. Обновления статусов задач
Отправляются при изменении статуса задачи в ClickUp:
```
✅ Обновление статуса задачи

📋 Задача: [Alex] Проверить отчет
👤 Исполнитель: Alex
📊 Статус: to do → complete

💬 Ответ: Да, выполнил

🔗 Открыть задачу
```

### 3. Уведомления об ошибках
Отправляются при возникновении ошибок в системе:
```
⚠️ Ошибка в системе напоминаний

❌ Ошибка: Ошибка при звонке на +1234567890

📝 Контекст: Задача: [Alex] Проверить отчет, Ошибка: Connection timeout
```

### 4. SMS уведомления
Отправляются при отправке SMS-напоминаний:
```
📨 Отправлено SMS-напоминание

📋 Задача: [Alex] Проверить отчет
👤 Исполнитель: Alex
📱 Телефон: +1234567890
```

## ⚙️ Настройка типов уведомлений

Вы можете отключить определенные типы уведомлений в `config.json`:

```json
{
  "telegram": {
    "enabled": true,
    "chat_id": "123456789",
    "notifications": {
      "task_reminders": false,      // отключить напоминания о задачах
      "status_updates": true,        // включить обновления статусов
      "call_notifications": true,    // включить уведомления о звонках
      "sms_notifications": false,    // отключить SMS уведомления
      "errors": true                 // включить уведомления об ошибках
    }
  }
}
```

## 🔒 Безопасность

1. **Никогда не коммитьте** `secrets.json` в Git
2. Убедитесь, что `secrets.json` добавлен в `.gitignore`
3. Храните токен бота в безопасном месте
4. Для продакшена используйте переменные окружения:

```bash
export TELEGRAM_BOT_TOKEN="your_bot_token"
export TELEGRAM_CHAT_ID="your_chat_id"
```

## 🐛 Устранение неполадок

### Бот не отправляет сообщения

1. **Проверьте токен бота:**
   ```bash
   curl https://api.telegram.org/bot<YOUR_TOKEN>/getMe
   ```
   Должен вернуть информацию о боте.

2. **Проверьте Chat ID:**
   - Убедитесь, что вы отправили боту хотя бы одно сообщение
   - Для групп: убедитесь, что бот добавлен в группу

3. **Проверьте логи:**
   ```bash
   tail -f var/logs/reminders_*.log
   ```
   Ищите строки с "Telegram" или "ERROR"

### Ошибка "Chat not found"

- Убедитесь, что Chat ID правильный
- Для групп: убедитесь, что бот не был удален из группы
- Отправьте боту сообщение и попробуйте снова

### Ошибка "Unauthorized"

- Проверьте правильность токена бота
- Убедитесь, что токен не содержит лишних пробелов

## 📚 Дополнительные возможности

### Отправка в несколько чатов

Если нужно отправлять уведомления в несколько чатов, можно указать массив:

```json
{
  "telegram": {
    "enabled": true,
    "chat_ids": ["123456789", "-987654321"],
    "notifications": { ... }
  }
}
```

**Примечание:** В текущей версии поддерживается только один chat_id. Для нескольких чатов нужно будет модифицировать код.

### Форматирование сообщений

Система использует HTML форматирование. Поддерживаются теги:
- `<b>текст</b>` - жирный
- `<i>текст</i>` - курсив
- `<code>текст</code>` - моноширинный
- `<a href="url">текст</a>` - ссылка

## 🔗 Полезные ссылки

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [BotFather](https://t.me/botfather)
- [Получение Chat ID](https://t.me/userinfobot)

## 💡 Примеры использования

### Уведомления только об ошибках

```json
{
  "telegram": {
    "enabled": true,
    "chat_id": "123456789",
    "notifications": {
      "task_reminders": false,
      "status_updates": false,
      "call_notifications": false,
      "sms_notifications": false,
      "errors": true
    }
  }
}
```

### Полный мониторинг

```json
{
  "telegram": {
    "enabled": true,
    "chat_id": "123456789",
    "notifications": {
      "task_reminders": true,
      "status_updates": true,
      "call_notifications": true,
      "sms_notifications": true,
      "errors": true
    }
  }
}
```
