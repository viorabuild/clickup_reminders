# 🔧 Готовая конфигурация Telegram

## Ваши данные Telegram:
- **Bot Token:** `<YOUR_TELEGRAM_BOT_TOKEN>`

---

## 📝 Инструкция по применению

### 1. Обновите `.venv/bin/secrets.json`

Откройте файл `.venv/bin/secrets.json` и добавьте секцию `telegram`:

```json
{
  "clickup": {
    "api_key": "ваш_существующий_ключ",
    "team_id": "ваш_существующий_team_id"
  },
  "twilio": {
    "account_sid": "ваш_существующий_sid",
    "auth_token": "ваш_существующий_token",
    "phone_number": "ваш_существующий_номер"
  },
  "openai": {
    "api_key": "ваш_существующий_ключ"
  },
  "telegram": {
    "bot_token": "<YOUR_TELEGRAM_BOT_TOKEN>"
  }
}
```

> ℹ️ Если хотите задать конкретный чат (например, для CI), добавьте поле `"chat_id": "<ваш_chat_id>"` в секцию `telegram` и экспортируйте переменную `TELEGRAM_CHAT_ID`.

### 2. Обновите `config.json`

Откройте файл `config.json` и добавьте секцию `telegram`:

```json
{
  "clickup_workspace_id": "ваш_workspace_id",
  "reminder_list_name": "Напоминания",
  "phone_mapping": {
    "ваше_имя": "+ваш_номер"
  },
  "working_hours": {
    "start": 9,
    "end": 18
  },
  "check_interval_minutes": 30,
  "call_timeout_seconds": 30,
  "max_retries": 2,
  "telegram": {
    "enabled": true,
    "notifications": {
      "task_reminders": true,
      "status_updates": true,
      "call_notifications": true,
      "sms_notifications": true,
      "errors": true
    }
  }
}
```

---

## ▶️ Запуск напоминаний

1. **Разово отправить задачи в Telegram** (используется в GitHub Actions):
   ```bash
   python3 send_telegram_reminders.py --verbose
   ```
2. **Запустить бота для интерактивных ответов** (long polling):
   ```bash
   python3 telegram_bot.py --initial-send --verbose
   ```
3. После запуска отправьте боту в личном чате команду `/start` — бот пришлёт актуальные напоминания с кнопками ✅/❌/🔄, а выбранный статус сразу обновится в ClickUp. Chat ID запишется в `var/telegram_chat_id.txt`, и скрипты смогут слать уведомления напрямую.

---

## ✅ Проверка настройки

После обновления файлов запустите тест:

```bash
cd /Users/danyanovich/code\ projects/clickup-rem
python3 test_telegram_integration.py
```

Вы должны увидеть в консоли:
```
============================================================
🧪 Тест Telegram интеграции
============================================================

📂 Загрузка конфигурации...
✅ Telegram включен в конфигурации
   Chat ID: <не указан>

🔧 Создание Telegram notifier...
✅ Telegram notifier успешно создан

🔌 Проверка подключения к Telegram API...
✅ Подключение к Telegram API успешно

📤 Отправка тестового сообщения...
✅ Тестовое сообщение успешно отправлено!
```

И в личный чат с ботом придут тестовые сообщения! 🎉

---

## 🔍 Проверка бота

Убедитесь, что:
1. ✅ Вы начали диалог с ботом и отправили `/start`
2. ✅ Бот может писать вам в личный чат
3. ✅ В файле `var/telegram_chat_id.txt` появилась запись с вашим chat_id

Проверить бота можно командой:
```bash
curl "https://api.telegram.org/bot<YOUR_TELEGRAM_BOT_TOKEN>/getMe"
```

Должен вернуть информацию о боте.

---

## 🚀 Готово к использованию!

После настройки система будет автоматически отправлять уведомления в личный чат с ботом:
- 📞 При инициации звонков
- ✅ При обновлении статусов задач
- ⚠️ При возникновении ошибок
- 📨 При отправке SMS
